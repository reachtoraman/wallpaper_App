class SquareModel {
  String category;
  List url;
  SquareModel(this.category, this.url);
}

class SingleContainerModel {
  String category;
  List url;
  SingleContainerModel(this.category, this.url);
}

class CircleListModel {
  String category;
  List url;
  CircleListModel(this.category, this.url);
}
